# Claude에 붙여넣을 UI 디자인 프롬프트

아래 `====` 사이 전체를 복사해서 Claude에게 붙여넣으세요.
받은 HTML을 `static/index.html`에 덮어쓰고 브라우저 새로고침하면 끝입니다.
(기존 파일은 `index_backup.html` 등으로 백업해두세요.)

---

```
====================================================================

기계 도면 자동 채점 웹앱의 프론트엔드를 새로 디자인해줘.
결과물은 **자체 완결된 단일 HTML 파일** 하나야 (CSS·JS 전부 인라인).

────────────────────────────────────────────────────────────
## 이 도구가 뭐냐

한국의 국가기술자격 **전산응용기계제도기능사** 실기시험을 준비하는 사람이
자기가 그린 CAD 도면을 올리면, 실제 채점기준에 따라 자동으로 점수를 매기고
"어디가 틀렸고 어떻게 고치는지"를 알려주는 도구야.

백엔드는 Autodesk Inventor를 실제로 구동해서 도면 안의 치수·공차·표면거칠기
기호·기하공차·주서를 진짜로 읽어. 눈속임이 아니라 실제 CAD 데이터 분석이야.

사용자: 공업고 학생, 직업훈련생, 시험 준비생, 기계과 강사.
20대 초반이 많고, 모바일로도 본다. UI 텍스트는 전부 한국어.

────────────────────────────────────────────────────────────
## 디자인 방향

**미래지향적 + 개발자 도구 느낌.** 구체적으로:

- 다크 테마 고정. CAD 도면 SVG가 어두운 배경(#212830)이라 그게 자연스럽다.
- **정밀 계측 장비 / 엔지니어링 콘솔** 분위기. 예쁜 대시보드보다
  "터미널과 오실로스코프 사이" 쪽. 데이터 밀도가 높아야 함.
- 숫자·코드·경로·치수는 등폭 글꼴 + `font-variant-numeric: tabular-nums`.
  본문 한글은 가독성 좋은 시스템 sans-serif.
- 강조색은 절제해서 형광/네온 계열 한두 개만. 나머지는 무채색.
  색은 심각도 구분에 우선 배정한다(아래 참조).
- 미묘한 디테일 환영: 얇은 그리드/스캔라인 텍스처, 발광 테두리, 모노스페이스
  라벨, 각진 모서리, 상태 표시등, 데이터 로딩 시 카운트업 애니메이션.
- **단, 읽히는 게 최우선.** 글로우로 텍스트를 뭉개거나, 대비를 낮추거나,
  장식 때문에 정보를 밀어내면 실패다. 시험 준비생이 오답을 읽는 화면이다.
- 애니메이션은 짧고 목적이 있을 것(로딩, 점수 채워지기, 펼침). 배경에서
  계속 움직이는 파티클 같은 건 넣지 마.

────────────────────────────────────────────────────────────
## 가장 중요한 요구사항: 처음 온 사람도 써볼 수 있어야 한다

지금 화면은 파일이 없으면 아무것도 없는 빈 화면이야. 처음 방문한 사람이
"이게 뭐 하는 건지, 뭘 올려야 하는지, 뭘 검사하는지" 모른 채 이탈해.

**분석 결과가 없는 초기 상태(empty state)가 이 화면의 절반이라고 생각하고
설계해줘.** 여기에 반드시 들어가야 할 것:

1. **한 줄로 뭔지 설명** — "도면을 올리면 전산응용기계제도기능사 채점기준으로
   자동 채점합니다" 수준의 명확한 문장.
2. **어떻게 쓰는지 3단계** — 파일 올리기 → 자동 분석 → 점수와 고칠 곳 확인.
3. **무엇을 검사하는지 미리 보여주기.** 이게 핵심이야.
   `GET /api/health`가 **실제 채점 항목과 배점, 검사 23개 목록**을 준다.
   이걸 그대로 렌더링해서 "이 도구는 이런 걸 본다"를 보여줘.
   배점표(투상도 30 / 치수 15 / 공차 10 / 표면거칠기 10 / 기하공차 10 /
   주서·표제란 8 / 재료 7)를 시각적으로 표현하면 그 자체로 학습 자료가 된다.
4. **오작(실격) 개념 안내** — 점수와 무관하게 떨어지는 조건 5가지가 있다는 걸
   눈에 띄게. 시험 준비생에게 제일 중요한 정보다.
5. **지원 형식 안내** — `/api/health`의 `supported` 배열을 그대로.

**금지:** 가짜 점수, 가짜 도면 이미지, 지어낸 예시 결과를 진짜인 것처럼
보여주지 마. 위 3·4번은 API가 주는 진짜 데이터라 정직하게 쓸 수 있다.
꼭 예시 화면을 보여주고 싶으면 "예시"라고 명확히 표시해.

────────────────────────────────────────────────────────────
## 절대 제약 (어기면 동작 안 함)

1. **외부 네트워크 리소스 전면 금지.** CDN 스크립트·웹폰트·외부 이미지 전부
   안 됨. CSS/JS는 전부 인라인. 아이콘은 이모지나 인라인 SVG로.
2. 예외 하나: `<script src="/static/vendor/three.min.js"></script>`
   three.js는 서버에 로컬로 들어있다. 3D 뷰어에 이걸 쓴다.
3. **API 경로·필드명을 절대 바꾸지 마.** 아래 스펙 그대로.
4. 파일명·품번에 한글이 많다. 깨지거나 잘리지 않게.
5. 모바일에서도 봐야 한다. 1080px 이하에서 1컬럼으로 자연스럽게 접힐 것.

────────────────────────────────────────────────────────────
## API 스펙 (실제 응답에서 그대로 뽑은 것)

### GET /api/health
{
  "ok": true,
  "supported": [".dwg",".dxf",".iam",".idv",".idw",".ipn",".ipt"],
  "inventor": true,        // false면 DWG/DXF만 분석 가능 -> 안내 필요
  "local": true,           // false = 원격 접속 -> 경로 입력 UI를 숨길 것
  "dwg_converter": true,   // false면 DWG 변환 불가, DXF만
  "oda": null,
  "exam": { "sheet": "A3", "third_angle": true },
  "checks": [              // 23개. 초기 화면과 검사 선택기에 쓴다
    { "id": "DQ_NO_SURFACE_SYMBOL",
      "label": "표면거칠기 기호가 아예 없음",
      "group": "오작",              // 묶는 키
      "group_label": "오작(실격)",   // 사람이 읽을 이름
      "default": true }
  ]
}

checks의 group 8종과 개수:
  오작                → 오작(실격)            5개
  DIMENSIONS         → 치수 기입              2개
  TOLERANCE          → 끼워맞춤 공차·치수공차   2개
  SURFACE            → 표면거칠기             3개
  GEOMETRIC          → 형상(기하)공차          3개
  NOTES_TITLE        → 주서·표제란·부품란       3개
  MATERIAL           → 재료 선택과 처리         1개
  PROJECTION_LAYOUT  → 투상도 선택과 배열       4개

### POST /api/analyze   (파일 업로드)
multipart/form-data
  file   : 파일
  checks : 켤 검사 id들을 콤마로 이은 문자열 (예 "EX_DIM_MISSING,EX_NO_HEAT")
           빈 문자열이면 "아무 검사도 안 함"으로 해석된다. 필드를 아예 빼면 전체.
허용: .ipt .idw .iam .idv .ipn .dxf .dwg .zip

### POST /api/analyze-path   (로컬 경로 분석)
application/json  { "path": "C:\\...\\3축.idw", "checks": ["EX_..."] }
- `/api/health`의 `local`이 false면 서버가 403으로 막는다. 그 경우 이 UI를
  아예 숨기고, 대신 "도면은 모델 파일과 함께 .zip으로 압축해 올리세요"를 안내.
- **왜 중요한가**: .idw 도면은 .ipt 부품 파일을 경로로 참조한다. 도면만 따로
  올리면 참조가 끊겨 형상 검사가 통째로 빠지고 "문제 없음"으로 잘못 나온다.
  같은 PC에서 쓸 때는 경로 입력이 가장 정확한 방법이라 눈에 띄게 배치할 것.

### GET /api/model/{job}   → 바이너리 STL (3D 미리보기용)

### 에러
4xx/5xx일 때 `{"detail": "..."}`. 그 메시지를 그대로 보여줄 것.

────────────────────────────────────────────────────────────
## 분석 응답 구조

{
  "job": "3ec19a6dba17",
  "file": "5커버(2개).idw",
  "kind": "drawing",         // part | drawing | assembly | dwg
  "props": { "part_number","material","designer","description",
             "revision","project","stock_number" },
  "standard": "기본 표준 (ISO)",
  "first_angle": false,      // true=제1각법, false=제3각법, null=모름
  "notes_text": ["주서 1.일반공차-가)가공부:KS B ISO 2768-m ...", ...],

  "summary": { "total":3, "fail":0, "error":1, "warn":2, "info":0 },

  "scorecard": {
    "auto_score": 49, "auto_max": 60, "percent": 81.7,
    "review_points": 30,        // 사람이 직접 봐야 하는 배점
    "disqualified": false,
    "disqualifiers": [],        // 오작 사유 문자열 배열
    "enabled_count": 23, "total_checks": 23,
    "items": [
      { "code":"PROJECTION_LAYOUT", "label":"투상도 선택과 배열",
        "max":30, "score":null, "mode":"review" },   // score=null -> 자동판정 불가
      { "code":"DIMENSIONS", "label":"치수 기입",
        "max":15, "score":11, "mode":"auto" }
    ],
    "summary": { ... }
  },

  "findings": [
    { "code": "EX_DIM_MISSING",
      "severity": "error",          // fail > error > warn > info
      "title": "치수 누락 의심 2곳",
      "detail": "치수가 붙지 않은 원/구멍이 있습니다. ... Ø33.0, Ø30.9",
      "fix": "해당 형상에 치수를 추가하세요. ...",
      "item": "DIMENSIONS",         // 어느 채점항목 소속인지
      "deduct": 4,                  // 이 지적으로 깎인 점수
      "where": { "sheet":"시트:1", "view":"뷰1", "sketch":"스케치1",
                 "feature":"모깎기1", "faces":[6,26],
                 "x_cm":58.4, "y_cm":28.1, "diameter_mm":33.0, "count":4 } }
  ],

  "stats": { "kind","sheets","dims","undimensioned","views","title_block",
             "sketches","sketches_under","holes","walls","interferences",
             "sick_features","mass_kg","volume_cm3","feature_count",
             "occurrence_count" },   // 있는 키만 온다

  "svg": "<svg ...>",        // 도면일 때만. 없으면 null
  "markers_placed": true,
  "marker_index": [ {"n":1,"diameter_mm":33.0,"sheet":"시트:1"} ],
  "model_url": "/api/model/3ec19a6dba17",   // 부품/조립일 때만, 없으면 null
  "model_error": null,
  "refs_ok": true,           // false = 모델 참조 끊김
  "svg_note": null
}

### severity 4단계 — 색 배정의 1순위
  fail  오작(실격). 점수와 무관하게 불합격. 가장 강하게.
  error 감점 큰 오류
  warn  경고
  info  참고

────────────────────────────────────────────────────────────
## 결과 화면에 반드시 있어야 할 것

1. **점수 카드 (최상단, 가장 눈에 띄게)**
   - `auto_score`/`auto_max`와 `percent`를 크게. 카운트업 애니메이션 좋다.
   - `disqualified`가 true면 점수 대신 **"오작(실격)"**을 압도적으로 크게
     띄우고 `disqualifiers` 목록을 보여준다. 이게 최우선 정보다.
   - `items`를 항목별 막대로. `score`가 null인 항목(투상도 30점)은
     "사람이 확인" 상태로 시각적으로 구분(빗금, 점선 등).
   - `review_points`가 별도 배점이라는 걸 명확히.
   - `enabled_count < total_checks`면 "검사 N개 꺼둠"을 표시.

2. **면책 문구** — "공식 채점이 아닙니다. 기호·값의 유무와 규격 적합성만
   자동 확인하며, 배점이 가장 큰 투상도 선택과 배열(30점)은 사람이 봐야
   판단됩니다." 작지만 반드시 있을 것.

3. **검사 선택기** — `/api/health`의 checks를 group별로 묶어 체크박스.
   전체 켜기 / 전체 끄기 / 기본값 버튼. 선택은 localStorage에 저장.
   검사를 끄면 그 감점도 사라진다는 걸 사용자가 알 수 있게.

4. **2D 도면 뷰어** (`svg`가 있을 때)
   - SVG를 그대로 삽입. 확대/축소/맞춤. 뷰어 영역만 따로 스크롤.
   - 도면에 **빨간 화살표와 번호**가 이미 그려져 있다.
     `findings` 중 `code === "DIM_MISSING"` 또는 `"EX_DIM_MISSING"` 항목이
     나온 순서대로 그 번호에 대응한다. 목록에 번호 배지를 붙여 짝을 보여줄 것.

5. **3D 뷰어** (`model_url`이 있을 때) — three.js로 바이너리 STL 렌더.
   드래그 회전 / 휠 확대 / 우클릭 드래그 이동 / 초기화 / 와이어프레임 토글.
   STL 바이너리 판별식: `84 + dv.getUint32(80,true)*50 === byteLength`.
   파일에 법선이 있으니 그대로 쓰고, 없으면 computeVertexNormals().
   재질은 금속 느낌 회청색, ambient + directional 2개 정도.

6. **지적사항 목록**
   - severity별 색. 제목은 항상 보이고 클릭하면 `detail`과 `fix`가 펼쳐진다.
   - **`fix`(고치는 방법)를 시각적으로 확실히 강조.** 이게 이 도구의 존재
     이유다. 단순 오류 목록이 아니라 "이렇게 고쳐라"가 읽혀야 한다.
   - `deduct`(깎인 점수)와 `item`(소속 채점항목)을 함께 표시하면 좋다.
   - `where`의 정보(시트/뷰/스케치/좌표/개수)는 작게 보조 표시.
   - severity 필터, 전체 펼치기/접기.

7. **refs_ok === false 경고 배너** — 눈에 띄게. 왜 형상 검사가 빠졌고
   (도면만 올려서 부품 참조가 끊김) 어떻게 해결하는지(zip 또는 경로 입력).
   이걸 놓치면 사용자가 잘못된 "문제 없음"을 믿는다.

8. **inventor === false 안내** — 이 서버는 DWG/DXF만 분석 가능하다고.

9. **분석 중 상태** — Inventor가 파일을 여느라 **10~60초** 걸린다.
   멈춘 걸로 오해하지 않게 진행 표시 + 왜 오래 걸리는지 설명.
   개발자 도구 느낌 살려서 단계 로그처럼 보여주는 것도 좋다(단, 실제로
   진행 정보를 주는 API는 없으니 가짜 퍼센티지는 만들지 말 것).

10. **파일 정보 패널** — `stats`를 한글 라벨로. 값이 있는 키만.

────────────────────────────────────────────────────────────
## 하지 말 것

- 로그인·회원가입·설정 페이지 (없는 기능)
- 여러 파일 동시 분석 (백엔드가 한 번에 하나)
- 시계열 차트, 추이 그래프 (그런 데이터가 없다)
- 실제로 동작하지 않는 버튼
- 가짜 진행률, 가짜 결과, 가짜 통계
- 배경에서 계속 도는 애니메이션

완성된 단일 HTML 파일 전체를 출력해줘.

====================================================================
```

---

## 붙여넣은 후

1. 받은 HTML을 `static/index.html`로 저장 (기존 파일 백업 권장)
2. 서버 재시작 없이 브라우저 새로고침만 하면 반영됨
3. 확인: 점수 카드 / 검사 선택기 / 도면 화살표 / 3D 회전 / 처음 화면 안내

동작이 깨지면 "절대 제약" 항목을 다시 짚어주세요. 가장 흔한 실수는
**CDN 링크를 넣는 것**과 **API 필드명을 임의로 바꾸는 것**입니다.

테스트용 경로:
```
C:\Users\smile\OneDrive\Desktop\캐드 파일\모델링, 도면\5커버(2개).idw   ← 49/60, 화살표 2개
C:\Users\smile\OneDrive\Desktop\캐드 파일\모델링, 도면\1본체.idw        ← 오작(실격)
C:\Users\smile\OneDrive\Desktop\캐드 파일\모델링, 도면\1본체.ipt        ← 3D 미리보기
```
