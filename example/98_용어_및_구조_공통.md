# 용어 및 코드 구조 (공통)

각 종목 폴더의 `41_용어집.md`는 이 문서를 요약 참조합니다. 여기서는 전체 구조를
한 번에 봅니다.

## 검사 파이프라인

```
DWG/DXF 업로드
  → dwg.py       파일 변환·파싱 (ezdxf, 필요시 dwg2dxf/ODA)
  → rules.py     11개 검사 함수 실행 → findings 목록
  → exam.py      채점 루브릭 적용, 오작(DQ) 판정 → summary
  → ai_review.py AI 검토 (투상도 배열 등 자동 판정이 어려운 항목)
```

## 종목별로 바뀌는 것 / 바뀌지 않는 것

| 구분 | 예시 | 종목별로 |
|---|---|---|
| 검사 로직 (rules.py 함수 본문) | 치수 상/하한 역전 판정 | 바뀌지 않음 — KS/물리 법칙 |
| 검사 로직 (exam.py 함수 본문) | 오작 판정 조건식 | 바뀌지 않음 |
| 임계값 상수 | 도면 크기, 최소 기호 개수, 표준 척도 목록 | 종목마다 다름 |
| 재질 사전 | GENERIC_MATERIALS 인접 목록 | 금형 계열은 확장 필요 |
| 루브릭 배점 | RUBRIC의 점수 | 공식 비공개, 종목마다 추정만 가능 |

이 분리가 [`99_도입_로드맵.md`](99_도입_로드맵.md) 0단계(구조 정리)의 근거입니다.

## 문서 세트 구조 (종목 1개당 47개)

| 번호대 | 구성 | 개수 |
|---|---|---|
| 01~09 | 개요~도입타당성결론 (핵심 판단) | 9 |
| 10~15 | KS 표준 세부 | 6 |
| 16~26 | rules.py 검사함수별 분석 | 11 |
| 27~31 | 오작(DQ) 조건별 분석 | 5 |
| 32~38 | 루브릭 항목별 세부 | 7 |
| 39~47 | 프로젝트관리(일정·리스크·용어·테스트 등) | 9 |

공통 문서 6개(`README`, `00`, `96`, `97`, `98`, `99`)를 더해 총 241개입니다.

공통 문서는 종목 폴더 밖, `example/` 최상단에 둡니다. 한 종목에 속하지 않고
종목을 가로질러 비교하거나(`00`, `96`) 판정 기준 자체를 정의하기(`97`, `98`) 때문입니다.
