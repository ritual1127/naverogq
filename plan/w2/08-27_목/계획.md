# 08-27 (목) · W2 3일차 — 인터뷰 3·4

| | |
|---|---|
| 주차 | **W2 · 사용자와 문제** · [주차 계획](../README.md) |
| 날짜 | 2026-08-27 (목) |
| 오늘의 한 줄 | 표본을 늘린다. 두 명 말은 우연일 수 있다. |
| 이번 주 산출물 | 사용자 인터뷰 5건 · 문제 정의 한 문장 |
| 오늘 담당 | 안대열 1건 · 김승준 1건 |
| 끝난 기준 | 인터뷰 4건 누적, 도면 라벨 파일 초안 |

## 시작 전 준비물

- 어제 기록
- `exam.py`의 `CHECKS` 목록

## 시간표

| 때 | 무엇 | 누가 |
|---|---|---|
| 방과 후 | 인터뷰 3 | 안대열 |
| 이어서 | 인터뷰 4 | 김승준 |
| 저녁 | 도면 라벨 정리 | 김승준 · 안대열 |

## 할 일

### 1. 학생 2명 더 인터뷰 — **안대열**  ·  예상 40분 (2명)

- 가능하면 다른 학년이나 다른 반에서 찾는다. 같은 반만 하면 같은 선생님한테 배운 얘기만 나온다.
- 어제 인터뷰에서 겹친 말이 있으면 **그 부분을 더 캐묻는다.**

**이렇게 되면 끝** — 인터뷰 4건 누적

**흔한 실수** — 어제와 똑같은 질문만 하는 것. 겹친 말은 더 캐물어야 한다

### 2. 받은 도면을 라벨 형식으로 정리 시작 — **김승준**  ·  예상 1시간

- `bench.py`가 읽는 JSON 형식: `{"파일명.dxf": ["EX_DIM_MISSING", "EX_NO_FIT"]}`
- 지적 코드는 `exam.py`의 `CHECKS`에서 확인한다.
- 정답 라벨은 **사람이 판단해서** 적는다. CADLens가 뱉은 결과를 그대로 정답으로 쓰면 측정이 자기 자신을 채점하는 꼴이 된다.
- 헷갈리는 항목은 선생님께 여쭤볼 목록으로 따로 뺀다 — 내일 인터뷰에서 물어본다.

**이렇게 되면 끝** — `labels.json`이 `bench.py`에서 읽힌다

**흔한 실수** — CADLens 결과를 그대로 정답으로 적는 것. 그러면 100%가 나오고 아무 의미가 없다

## 오늘 남길 것

| 어디 | 무엇 |
|---|---|
| `docs/interviews.md` | 인터뷰 3 · 4 기록 |
| `labels.json` | 실제 도면 정답 라벨 초안 |

## 달성

❌ — `⬜` 안 함 · `✅` 함 · `🔺` 반쯤 · `❌` 못 함

사람별로 무엇을 했는지는 [역할 분담](../../역할분담/README.md)에도 같이 적습니다.

## 메모

| 항목 | 담당 | 달성 | 무엇을 했나 |
|---|---|---|---|
| 1. 학생 2명 더 인터뷰 | 안대열 | ❌ | 사람 몫. 22:50 현재 [`docs/interviews.md`](../../../docs/interviews.md)에 들어온 인터뷰가 없다. 누적 0건 |
| 2. 받은 도면을 라벨 형식으로 정리 시작 | 김승준 | ❌ | 사람 몫인 데다 **재료가 없다.** [`실제도면/`](../../../실제도면/README.md)에 도면 0장, `labels.json` 은 `{}` 그대로 |

**끝난 기준 두 개가 다 0이다.** "인터뷰 4건 누적"은 0/4, "도면 라벨 파일 초안"은
도면이 0장이라 시작조차 못 했다. 오늘 두 항목은 전부 사람만 할 수 있는 일이고,
대신 했다고 적으면 기록이 거짓이 된다.

**2번은 사람 몫이라서만 막힌 게 아니다.** 어제(08-26) 안대열이 도면을 받아야 오늘
김승준이 라벨을 붙인다. 어제 것이 안 들어와 오늘 것이 통째로 멈췄다. 하루 밀린 게
아니라 **이틀치가 같이 밀렸다.** [P10](../../문제점/P10-인터뷰0건.md)에 적었다.

**준비물 두 가지는 둘 다 있었다.** 어제 기록(08-26 계획 메모)과 `exam.py` 의 `CHECKS`.
`CHECKS` 24개를 [`실제도면/README.md`](../../../실제도면/README.md) 의 코드 표와 한 줄씩
대조했고 **코드·설명 24개가 전부 일치**했다. 라벨을 붙일 사람이 볼 표는 틀린 데가 없다.

**인터뷰 폼도 0건이다.** 어제 세운 https://cadlens-interview.pages.dev 에
`SELECT COUNT(*) FROM answers` 를 돌려 **0** 을 확인했다. 대면이 안 되면 폼으로라도
받자고 만든 것인데 주소를 아직 아무에게도 뿌리지 않아 들어올 데가 없다.
만드는 것과 돌리는 것은 다른 일이었다.

**대상표도 아직 3/5 다.** [인터뷰 대상표](../08-25_화/인터뷰대상표.md) — 박주호 ·
박신후 · 손선생님. 2명과 **약속 시간 전부**가 비어 있다. 08-25 에서 넘어온 뒤
사흘째 그대로다. 약속이 없으니 인터뷰 날짜도 못 잡는다.

**남은 날이 4일이다.** W2 산출물은 인터뷰 5건이고 마감은 08-31(월)이다.
08-28 · 29 · 30 · 31 에 5건을 다 해야 한다.

**저장소는 초록이다.** `python -m pytest test_rules.py -q` **17개 통과**.
오늘 코드는 건드리지 않았다.


## 못 끝냈으면

라벨 정리가 안 끝나면 08-29 오전으로

- **학생 2명 인터뷰**(안대열) — 08-28 로 넘김. 08-28 은 원래 선생님 인터뷰(장우영)라
  같은 날 셋이 된다
- **도면 라벨 정리**(김승준) — 도면이 들어오는 날 그날. 계획대로 08-29 오전이지만
  도면이 0장이면 그날도 못 한다
- **인터뷰 1 · 2**(박지완) — 08-26 에서 08-27 로 넘어왔고 오늘도 못 해 08-28 로 다시 넘김
- **도면 파일 받기**(안대열) — 08-26 에서 넘어옴. 인터뷰 자리에서 같이
- **인터뷰 대상 2명 · 약속 시간**(장우영) — 08-25 에서 넘어옴. 사흘째. 08-28 인터뷰 전까지
- **인터뷰 폼 주소 뿌리기**(전원) — 08-28. 만들어 두고 안 뿌리면 0건이다
- **팀 페이지 W1 산출물 업로드**(안대열) — 08-24 에서 넘어옴. 08-31 제출 전까지
