# 08-23 (일) · W1 6일차 — 백지 렌더 고치고 편차 다시 재기

| | |
|---|---|
| 주차 | **W1 · 킥오프 — 미션과 목표** · [주차 계획](../README.md) |
| 날짜 | 2026-08-23 (일) |
| 오늘의 한 줄 | 어제 찾은 백지 렌더를 고친다. 그림이 백지면 그 위의 모든 개선이 헛수고다. |
| 이번 주 산출물 | 팀 목표 1장 |
| 오늘 담당 | 장우영 1건 · 안대열 1건 · 김승준 1건 · 박지완 1건 |
| 끝난 기준 | `sample_075em07z.dwg` 렌더가 백지가 아니고, 그 상태의 5회 점수 폭이 문서에 적혀 있다 |

## 시작 전 준비물

- `ai_review.py` 열어 두기
- `samples/sample_075em07z.dwg`
- 어제 잰 점수 목록 (0, 18, 18, 18, 18)

## 시간표

| 때 | 무엇 | 누가 |
|---|---|---|
| 오전 | 원인 확인 · 고치기 | 장우영 |
| 오후 | 눈으로 확인 · 다시 측정 | 안대열 |
| 저녁 | 감점 폭 상한 · 문서 갱신 | 김승준 |

## 할 일

### 1. `render_png`가 도면 단위를 반영하게 고치기 — **장우영**  ·  예상 2~3시간

- 위치: `ai_review.py`의 `render_png()`. 지금은 `dlayout.Page(0, 0, ...)`로 도면 범위를 **그대로 밀리미터로** 보고 페이지를 만든다.
- `sample_075em07z.dwg`의 `$EXTMIN`~`$EXTMAX`는 약 0.12 × 0.12다. 여기에 사방 5mm 여백을 붙이면 페이지가 거의 전부 여백이 되고 그림은 1픽셀 밑으로 짜부라진다.
- 고치는 방향 두 가지 — ① `$INSUNITS`를 읽어 mm로 환산(1=인치, 4=mm, 6=m 등) ② 단위를 알 수 없으면 도면 범위에 맞춰 페이지를 채우도록 배율을 준다.
- A3(420 × 297)로 그린 도면이 지금처럼 멀쩡히 나오는지도 같이 확인할 것. **회귀 주의.**

**이렇게 되면 끝** — `render_png`가 만든 그림이 201바이트가 아니고, A3 도면도 그대로 나온다

**흔한 실수** — 단위 환산만 고치고 A3 도면을 안 돌려 보는 것. 멀쩡하던 게 깨지면 더 나쁘다

### 2. 고친 그림을 저장해 눈으로 확인 — **안대열**  ·  예상 30분

- `ai_review.render_png(경로)` 결과를 파일로 써서 실제로 열어 본다.
- **파일 크기만 보고 넘어가지 말 것.** 201바이트면 백지지만 5KB라고 도면이 다 보이는 건 아니다.
- `samples/` 세 개 전부 확인한다.

**이렇게 되면 끝** — 그림 파일을 열어서 도면이 보인다

**흔한 실수** — 파일 크기만 보고 넘어가는 것. 5KB여도 선 몇 개만 그려진 경우가 있다

### 3. `variance.py`로 다시 재기 — **김승준**  ·  예상 1시간

- `python variance.py samples/sample_075em07z.dwg -n 5`
- 진짜 도면을 봤을 때 지적이 무엇으로 바뀌는지가 핵심이다. "투상도 전체 누락"이 사라져야 정상이다.
- 폭이 줄었으면 줄었다고, 안 줄었으면 안 줄었다고 `docs/accuracy.md`에 적는다.

**이렇게 되면 끝** — 지적이 "투상도 전체 누락"에서 실제 도면 내용으로 바뀌었다

**흔한 실수** — 점수 폭만 보고 지적 내용을 안 읽는 것

### 4. 감점 폭 눌러 주기 — **박지완**  ·  예상 1시간

- 프롬프트는 "형상 표현이 불가능할 정도의 누락: 8~12점"이라고 범위를 줬는데 30점을 다 깎았다.
- 스키마에서 `deduct` 상한을 걸거나 `_to_findings()`에서 항목당 상한을 둔다.
- **상한을 걸었다는 사실을 문서에 적을 것.** 안 적으면 나중에 왜 점수가 안 내려가는지 모른다.

**이렇게 되면 끝** — 같은 지적에 30점을 다 깎는 판정이 안 나온다

**흔한 실수** — 상한을 걸어 놓고 문서에 안 적는 것. 나중에 왜 점수가 안 내려가는지 모른다

## 오늘 남길 것

| 어디 | 무엇 |
|---|---|
| `ai_review.py` | render_png 단위 처리 |
| `docs/accuracy.md` | 고친 뒤 편차 재측정 값 |
| `plan/w1/기록.md` | 일요일 몫 달성 표시 |

## 막히면

- 단위 환산이 복잡하면 오늘은 **범위에 맞춰 채우기**만 해도 된다. 백지만 아니면 오늘 목적은 달성이다.
- 무료 할당량이 마르면 측정은 내일로 미루고 렌더 수정까지만 끝낸다.

## 달성

🔺 — `⬜` 안 함 · `✅` 함 · `🔺` 반쯤 · `❌` 못 함

사람별로 무엇을 했는지는 [역할 분담](../../역할분담/README.md)에도 같이 적습니다.

## 메모

**08-22 밤에 당겨서 했습니다.** 일요일에 시간이 안 나서 토요일에 몰아 했습니다.

- 렌더 수정 완료 — `dwg.detect_mm_per_unit`을 그대로 써서 도면 단위를 mm로 환산한다.
  `sample_075em07z.dwg` 201바이트 → 283,550바이트. 그림을 열어서 도면이 보이는 것까지 확인.
- **`sample_autocad.dxf`도 백지였다.** 편차 0으로 보였던 게 안정적이어서가 아니라 매번 같은
  백지를 봤기 때문이었다.
- 감점 상한 `MAX_SINGLE_DEDUCT = 12` 추가. 30점을 통째로 깎는 판정을 막는다.
- 자동 테스트 15개 통과.
- **다시 쟀더니 편차는 안 줄었다.** 0, 0, 5, 9, 18 — 폭 18점 그대로, 중앙값은 18점에서
  5점으로 내려갔다. 백지일 때는 지적이 1건으로 일정했는데 진짜 그림을 보자 1~3건으로 흔들린다.
  **편차의 원인은 백지가 아니라 프롬프트와 모델이었다.**
- 그림을 열어 보니 이 표본은 기계 부품 도면이 아니라 무늬 도면이었다. "투상도가 없다"는 판정
  자체는 틀리지 않았을 수 있다. **편차 측정은 실제 수험생 도면으로 다시 해야 한다.**

**계획에 없었는데 08-23 낮에 한 것** — 배포 서버에서 세 언어 번역만 계속 429로
막혔다. 도면 한 장에 AI를 세 번(채점 · 후속 답변 · 번역) 부르는데 순서상 마지막인
번역이 먼저 굶는 구조였다. 후속 답변과 번역을 `_enrich()` 한 호출로 합쳐 도면당
호출을 3회에서 2회로 줄였다. 합치면서 모델이 지어내는 조·항·장 번호를 후처리로
걷어내는 `_drop_clause_numbers()`도 넣었다. 자동 테스트 16개 통과(검사 1개 추가).
**배포 서버에서 429가 실제로 사라지는지는 할당량이 회복돼야 확인할 수 있다.**
[P08](../../문제점/P08-번역429.md)

🔺로 둔 이유 — 렌더 수정과 상한은 끝났지만 **편차를 줄이는 일은 못 했다.**

## 못 끝냈으면

편차 줄이기는 W2로 넘긴다. 실제 수험생 도면을 받은 뒤(08-26~28) 다시 재고,
그때도 흔들리면 여러 번 물어 중앙값을 쓰는 방식을 검토한다.
